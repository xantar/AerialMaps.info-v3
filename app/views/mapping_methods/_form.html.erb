<%= form_for(@mapping_method) do |f| %>
  <% if @mapping_method.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mapping_method.errors.count, "error") %> prohibited this mapping_method from being saved:</h2>

      <ul>
      <% @mapping_method.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
